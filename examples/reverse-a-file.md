Write a small program `reverse.py` that reads a file path as its first argument
and prints the file's lines in reverse order (last line first). Create a sample
input file, run your program against it, and confirm the output is correct before
finishing.
